name: Deploy x0x.md Worker

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      worker_name:
        description: "Optional worker name override for preview deploys"
        required: false
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Deploy Worker (default name)
        if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.worker_name == '' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: deploy

      - name: Deploy Worker (named preview)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.worker_name != '' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: deploy --name ${{ github.event.inputs.worker_name }}
