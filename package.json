{
  "name": "x0xmd",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check:drift": "node --test test/drift-contract.test.js",
    "ci": "npm run test && npm run check:drift && npm run deploy -- --dry-run",
    "security:audit": "npm audit --audit-level=low",
    "dev": "npm exec wrangler -- dev",
    "deploy": "npm exec wrangler -- deploy"
  },
  "devDependencies": {
    "wrangler": "^4.69.0"
  }
}
