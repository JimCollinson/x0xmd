# Plan 01-01 — Bootstrap ADR governance

Status: Approved; execution in progress

## Objective

Prepare a reviewable attended branch that installs the consumer ADR system from
`WithAutonomi/adr-standard@0b36be07b4730c158eaed3655b551318c81352bf`, adds seven substantive Proposed retrospective ADRs, and corrects the stale authority notice in `src/html.js` without changing runtime behaviour.

## Sources

- Current tracked repository files and Git history through `origin/main@7de2813459cff504e176d364cf7f52cc5ab85ea4`.
- `https://github.com/WithAutonomi/adr-standard` founding merge `0b36be07b4730c158eaed3655b551318c81352bf`: `kit/README.md`, `kit/AGENT-GUIDANCE.md`, all direct kit payload files, and `docs/spec/BOOTSTRAP.md`.
- Historical x0x Agent Onboarding `DECISIONS.md`, `SPEC.md`, `VISION.md`, and `planning/STATE.md` (read-only evidence).
- x0xmd PRs #1–#3 and the repository commits cited by the approved slice.

## Live Instruction and Execution Mode

Jim Collinson declared `attended mode` for this run on 2026-08-06. This live instruction overrides any unattended default. Stop and report rather than applying the unattended decision screen when a stop condition fires.

## Scope

1. Record this plan and current state.
2. Install the eight canonical direct consumer-kit files at their supported paths and modes; merge the kit guidance into a new repository-specific `AGENTS.md`.
3. Draft ADR-0001 through ADR-0007 with the exact consumer template structure, Proposed status, real options/trade-offs, and validation grounded in the approved evidence.
4. Remove only `src/html.js:1-3`, then update state with truthful verification and review status.

## Out of Scope

- `.adr-kit.yaml`, consumer-root legal files, and changes to `.github/workflows/deploy.yml`.
- Route, caching, network, trust-contract, frontend, deployment, build, test, or environment behaviour.
- ADR-0008/0009/0010 placeholders.
- Modification or staging of pre-existing `.artifacts/` and `.claude/`.
- Push, PR creation, merge, publish, release, production deployment, or autonomous ADR acceptance.

## Verification and Rigor

- **Meaningful work-unit:** Yes. The slice introduces durable ADR governance and seven architectural records.
- **Rigor:** Full gauntlet, with review cadence per work-unit.
- **Local gate:** No `.gsd/gate.sh` exists. Run `python3 scripts/adr-governance.py`, source blob/mode verification, `git diff --check origin/main...HEAD`, and status/commit-scope inspection.
- **CI arbiter:** The new ADR Governance workflow is the structural CI arbiter for ADR changes once a PR exists. No general quality CI arbiter exists; `deploy.yml` is deployment, not validation.
- **Review:** Clean-context, adversarial, and Craft Review are required per meaningful unit before Jim's attended acceptance checkpoint. Record any not-yet-run lane honestly; do not infer green.
- **Honesty:** Do not alter tests, gates, deployment, builds, wrappers, or environment to obtain a pass. Readiness evidence must be reproducible from committed branch state.

## Tasks

### Task 1 — Record ADR adoption plan (auto)

Create the minimal durable plan and state, including attended mode, source pin, rigor, CI boundaries, queue, and stop conditions.

Verify: inspect the new files and run `git diff --check`.

Done when: the execution boundary is committed without claiming unrun evidence.

### Task 2 — Install ADR governance kit (auto)

Install the eight direct payload files byte-exact with required modes and create repository-specific `AGENTS.md` from the mergeable guidance fragment.

Verify: compare destination blobs and modes to the pinned source; run the installed governance script.

Done when: only the approved kit, workflow, and agent guidance are committed.

### Task 3 — Add Proposed retrospective ADRs (auto)

Draft ADR-0001 through ADR-0007 from the pinned template and approved evidence. Keep every record Proposed and preserve unresolved validation questions explicitly.

Verify: run the governance script and inspect all statuses, headings, evidence, and scope boundaries.

Done when: all seven substantive records are committed and none is Accepted.

### Task 4 — Correct frontend source notice and final state (auto)

Remove only the stale generated-source comment at `src/html.js:1-3` and update `planning/STATE.md` with actual local evidence and pending review/CI state.

Verify: run all required final commands, inspect the full branch diff/status, and confirm `.artifacts/` and `.claude/` remain untracked and uncommitted.

Done when: runtime frontend bytes after the comment are unchanged, evidence is recorded truthfully, and the final task is committed.

## Stop Conditions

Stop and report if the work requires an ADR/spec decision beyond these approved Proposed drafts; any architecture, public API, storage, crypto, networking, trust, data-model, or operational invariant change; another CI/test/deploy/build/environment mechanism change; a new dependency; destructive action; scope expansion; push/PR/merge/publish/deploy; or ADR acceptance.

## Next Checkpoint

After implementation and local verification, return the four atomic commits and evidence for independent per-unit review and Jim's attended decision on whether to create a PR.
